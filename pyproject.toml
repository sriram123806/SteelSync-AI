[project]
name = "steelsync-final"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "band-sdk[langgraph]>=1.0.0",
    "langchain-openai>=1.3.2",
    "python-dotenv>=1.2.2",
]
